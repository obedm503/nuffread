import * as React from 'react';
import { RouteComponentProps } from 'react-router';
import { Container } from 'bloomer';

export const Register: React.SFC<RouteComponentProps<never>> = () => (
  <Container>Register</Container>
);
