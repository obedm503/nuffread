import * as React from 'react';
import { RouteComponentProps } from 'react-router';
import { Container } from 'bloomer';

export const Pricing: React.SFC<RouteComponentProps<never>> = () => (
  <Container>public pricing page</Container>
);
