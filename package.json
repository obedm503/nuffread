{
  "private": true,
  "workspaces": [
    "api",
    "web"
  ],
  "scripts": {
    "dev": "nps dev",
    "build:api": "NODE_ENV=production nps clean && nps build.types && nps build.api",
    "heroku-postbuild": "npm run build:api",
    "build:web": "NODE_ENV=production nps clean && nps build.types && nps build.web",
    "build": "nps build",
    "start": "nps start"
  },
  "husky": {
    "hooks": {
      "pre-commit": "if [ $(git rev-parse --abbrev-ref HEAD) = 'master' ]; then echo 'cannot commit straight to master' && exit 1; fi"
    }
  },
  "dependencies": {
    "dotenv-safe": "^8.1.0",
    "nps": "^5.9.12",
    "nps-utils": "^1.5.0"
  },
  "devDependencies": {
    "@graphql-codegen/cli": "^1.9.1",
    "@graphql-codegen/typescript": "^1.9.1",
    "husky": "^3.0.5"
  },
  "resolutions": {
    "graphql": "14.5.8"
  },
  "engines": {
    "node": "10.16.2",
    "yarn": "1.19.2"
  }
}
