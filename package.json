{
  "private": true,
  "workspaces": [
    "api",
    "web"
  ],
  "scripts": {
    "dev": "nps dev",
    "build:api": "nps clean && nps build.types && nps build.api",
    "heroku-postbuild": "npm run build:api",
    "build:web": "nps clean && nps build.types && nps build.web",
    "build": "nps build"
  },
  "dependencies": {
    "dotenv-safe": "^6.1.0",
    "nps": "^5.9.3",
    "nps-utils": "^1.5.0"
  },
  "devDependencies": {
    "@gql2ts/types": "^1.9.0",
    "gql2ts": "^1.7.1",
    "nodemon": "^1.19.1"
  },
  "resolutions": {
    "graphql": "14.5.4"
  }
}
