{
  "private": true,
  "workspaces": [
    "api",
    "web"
  ],
  "scripts": {
    "dev": "nps dev",
    "build:api": "NODE_ENV=production nps deploy.api",
    "heroku-postbuild": "npm run build:api",
    "build:web": "NODE_ENV=production nps deploy.web",
    "build": "nps build",
    "start": "nps start"
  },
  "husky": {
    "hooks": {
      "pre-commit": "if [ $(git rev-parse --abbrev-ref HEAD) = 'master' ]; then echo 'cannot commit straight to master' && exit 1; fi"
    }
  },
  "dependencies": {
    "dotenv-safe": "^8.1.0",
    "nps": "^5.9.12",
    "nps-utils": "^1.5.0"
  },
  "devDependencies": {
    "@graphql-codegen/cli": "^1.11.0",
    "@graphql-codegen/typescript": "^1.11.0",
    "@graphql-codegen/typescript-resolvers": "^1.11.0",
    "husky": "^3.0.5"
  },
  "engines": {
    "node": "12.16.1",
    "yarn": "1.22.4"
  }
}
